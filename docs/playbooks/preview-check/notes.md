# 겪은 실패와 대응

| 날짜 | 증상 | 원인 | 바꾼 것 | 재확인 근거 |
|---|---|---|---|---|
| 2026-09-17 | `/plan/[id]` 계열만 404, 랜딩은 200 | **Supabase 무료 플랜이 일주일 활동 없으면 잠든다** | 랜딩만 보고 판정하지 않는다. `check-urls.sh` 가 DB 읽는 화면을 따로 구분해 검사 | Resume 후 샘플 초안 4개 전부 200 (이슈 #71) |
| 2026-09-18 | 미리보기가 302 로 튕김 | Vercel Authentication 이 켜져 있었다. **팀 멤버만 볼 수 있다** | 302 면 `Location` 을 읽어 `sso-api` 인지 구분해 원인을 찍는다 | 끈 뒤 같은 주소가 200 (이슈 #68) |
| 2026-09-19 | PR #59 설명과 실제 변경이 달랐다 | 설명은 "PC 는 안 건드렸다" 인데 diff 는 `md:` 크기 3개를 지웠다 | **설명을 믿지 않고 `measure.js` 로 본 배포와 브랜치를 같은 폭에서 잰다** | 1440px: 본 배포 15.68px vs 브랜치 10.4px |
| 2026-09-19 | 랜딩 정거장 구간을 캡처하면 빈 화면 | 랜딩이 **스크롤 연동(sticky) 카드**라, DOM 상 보이는데도 캡처 시점에 되돌아간다 | 크기·간격 판정은 캡처가 아니라 `measure.js` 수치로 한다. 캡처가 필요하면 **미확인**으로 남긴다 | PR #59 리뷰에 그렇게 기록 |
