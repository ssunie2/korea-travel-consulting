# 출시 전에 되돌릴 것

> **이 사이트는 일부러 손님에게 안 보이는 상태로 만들어져 있다.**
> 검색을 막아뒀고, 화면 글은 한국어로 띄워뒀고, 무료 등급 서비스를 쓰고 있다.
> 하나하나가 의도된 선택이고, **손님을 받기 전에 전부 되돌려야 한다.**
>
> 하나만 빠뜨려도 결과가 크다 — `LANG` 을 안 되돌리면 **해외 손님 전원이 한국어 화면을 본다.**
>
> 마지막 확인: 2026-09-19. 줄 번호는 그날 실제로 확인한 값이다.

---

## ⚠️ 먼저 — 돈을 받는 순간 약관 위반이 되는 것 넷

**$25 를 처음 받기 전에 넷 다 끝나야 한다.** 하나라도 남으면 "비상업용 등급으로 상업 서비스를 돌리는" 상태다.

| | 무엇 | 지금 | 해야 할 것 | 누가 |
|---|---|---|---|---|
| ☑ | **Vercel Hobby = 비상업용** | — | 뮤조 팀(Pro)으로 이전 | **끝남** (2026-09-18) |
| ☐ | **AI 무료 등급** | Gemini 무료 | 유료 등급 또는 OpenAI 전환(PR #65) | 아무나 |
| ☐ | **Open-Meteo 무료 = 비상업용** | `lib/weather.ts:4` | 기상청 단기예보 API 또는 유료 등급 | 아무나 |
| ☐ | **Supabase 무료 = 일주일 쉬면 정지** | 무료 플랜 | Supabase Pro (월 $25) | 선경 |

**AI 무료 등급이 특히 무겁다.** 손님이 폼에 적는 **할랄·알레르기·휠체어는 종교·건강 정보**인데, 무료 등급은 넣은 내용을 모델 개선에 쓰고 사람이 볼 수 있다. (`lib/types.ts` · `supabase/migrations/20260824120000_plan_form_answers.sql` 에 같은 경고가 두 번 적혀 있다)

**Supabase 가 잠들면 어떻게 되나** — `/plan/[id]` 계열이 전부 404 가 되는데 **랜딩은 200 이라 정상으로 보인다.** 2026-09-17 에 실제로 겪었다 (이슈 #71). 손님에게 이메일로 보낸 초안 링크가 죽는 것은 환불 사유다.

---

## 1. 코드에서 되돌릴 것

| # | 파일 | 지금 | 출시 때 |
|---|---|---|---|
| 1 | `lib/copy.ts:12` | `export const LANG: "ko" \| "en" = "ko"` | **`"en"` 으로. 한 글자.** `<html lang>` 도 이걸 따라가므로 같이 넘어간다 |
| 2 | `app/layout.tsx:28` | `robots: { index: false, follow: false }` | **그 줄 삭제** |
| 3 | `app/robots.ts:15` | `rules: { userAgent: "*", disallow: "/" }` | ⚠️ **파일을 지우지 마라.** `allow: "/"` 로 바꾸고 `sitemap` 주소를 넣는다 (아래 참고) |
| 4 | `app/page.tsx:340` | 랜딩의 샘플 초안 4개가 **한국어** | **영어 초안을 새로 만들어 아이디를 갈아끼운다.** 안 하면 카드 제목만 영어가 되고 눌러 들어간 초안은 한국어로 남는다 |
| 5 | `app/privacy/page.tsx:14` | `const CONTACT = "hello@example.com"` | **진짜 이메일.** 이 값이 문서 안 3군데(`mailto:`)에 쓰인다 |
| 6 | `app/privacy/page.tsx:12·335` | 사업자 상호·대표자·등록번호·주소가 빈칸 | 사업자등록 후 채운다 |
| 7 | `public/seoul-blue-hour.jpg` | 448KB, **아무 데서도 안 쓴다** | 삭제 |

### 3번이 왜 "삭제" 가 아닌가

`plans/002` 에는 "파일 삭제" 로 적혀 있었지만 **이슈 #69 에서 뒤집혔다.**
파일을 지우면 `robots.txt` 자체가 안 만들어지고, 그러면 **검색엔진에 sitemap 위치를 알려줄 곳이 사라진다.**
`allow: "/"` + `sitemap` 주소를 넣는 편이 낫다. GPTBot·ClaudeBot·PerplexityBot 을 따로 적을 필요는 없다 — 전체 허용이면 자동으로 포함된다.

### 5번이 왜 무거운가

지금 손님이 "제 정보를 지워주세요" 를 보내면 **아무 데도 도착하지 않는다.**
그런데 처리방침은 **"무료로, 30일 안에 처리해 드립니다"** 라고 약속하고 있다.

---

## 2. 대시보드에서 할 것 (코드 밖)

| # | 무엇 | 어디 | 누가 |
|---|---|---|---|
| 8 | **Vercel 미리보기 보호를 다시 켠다** | Vercel → Settings → Deployment Protection → Vercel Authentication → **Enabled** | **뮤조만** |
| 9 | **Supabase Pro 인지 확인** | Supabase 대시보드 | 선경 |
| 10 | **AI 월 사용 한도가 걸려 있는지 확인** | OpenAI / Google AI Studio | **뮤조만** |
| 11 | **`ADMIN_PASSWORD` 를 새 값 + Sensitive 로** | Vercel → 환경변수 | **뮤조만** |
| 12 | **안 쓰는 AI 키 삭제** | Vercel → 환경변수 | **뮤조만** |

### 8번이 왜 중요한가

2026-09-18 에 **일부러 껐다.** 선경이 팀 멤버가 아니라서 켜져 있으면 PR 미리보기를 못 열기 때문이다 (이슈 #68).

**미리보기는 본 배포와 같은 환경변수를 쓴다 = 진짜 DB 를 본다.**
지금은 손님 자료가 없어 열어둬도 되지만, **손님을 받기 시작하면 공개 미리보기는 그 자체가 사고다** — 손님 초안이 링크만으로 열린다.

**이 항목을 잊는 것이 이 목록에서 가장 비싼 실수다.**

---

## 3. 서류·계약 (제일 오래 걸린다)

**코드가 아니라 서류라서, 우리가 아무리 빨라도 심사 기간은 못 줄인다. 가장 먼저 시작해야 한다.**

```
사업자등록  →  통신판매업 신고  →  토스페이먼츠 계약  →  해외카드 별도 심사
```

| # | 무엇 | 메모 |
|---|---|---|
| 13 | **사업자등록** | 공동사업자 50:50. 동업계약서 필요 |
| 14 | **통신판매업 신고** | 사업자등록 후 |
| 15 | **취소·환불 규정 + 이용약관** | **표시 의무가 있고 PG 심사에서도 본다.** 지금 페이지 자체가 없다 |
| 16 | **토스페이먼츠 계약** | 이슈 #23 |
| 17 | **해외카드 심사** | **손님이 전부 해외 여행객이라 이게 안 끝나면 결제가 무의미하다.** 카드사 심사가 따로 붙는다 |
| 18 | **상표 검색 + 도메인** | `vercel.app` 주소로 출시하지 않는다. 이름이 확정돼야 도메인을 사고, 도메인이 있어야 sitemap·canonical 을 쓴다 |
| 19 | **랜딩 사진 5장의 출처·라이선스 확인** | `public/landing/*.webp` 5장. **코드에 출처 기록이 없다.** 돈 받는 사이트의 첫 화면이다 |

---

## 4. 코드에 없지만 손님에게 약속한 것

`/plan/[id]/consult` 화면이 이렇게 말한다.

> "전체 일정은 **$25** 입니다. **결제 링크와 일정을 이메일로 보내드립니다**"

**둘 다 코드가 없다.**

| # | 무엇 | 지금 |
|---|---|---|
| 20 | **결제** | 코드 0줄. 이슈 #23 |
| 21 | **이메일 발송** | 코드 0줄 (`resend`·`nodemailer`·`sendgrid`·`smtp` 전부 0건). 지금은 `/admin` 에서 사람이 손으로 보낸다 |
| 22 | **`/plan/[id]/full` 에 결제 확인 장치** | 검사가 "전체 일정이 만들어졌나" 하나뿐이다. **한 번 만들어지면 초안 주소 뒤에 `/full` 만 붙이면 열린다** |

⚠️ **22번 관련 — 랜딩에 걸린 샘플 초안 4개에 실수로 전체 일정을 만들면 $25 짜리 물건이 통째로 공개된다.** 그 아이디들은 공개 저장소에 적혀 있다.

⚠️ **21번은 규칙 3장 1번 6조와 닿는다** — *"사람이 상담하지 않는다. 입력부터 문서 제공까지 전 과정이 자동이다."* 사람이 손으로 메일을 보내는 것이 그 전제를 깨는지 둘이 판단할 것.

---

## 5. 마지막 — 출시 당일 확인

- [ ] `bash docs/playbooks/preview-check/files/check-urls.sh` → **종료코드 0**
- [ ] **폼을 실제로 채워 초안이 나오는지 확인** ← 위 검사는 화면이 뜨는지만 본다. 이건 따로 해야 한다
- [ ] 위 1~12 번을 한 줄씩 다시 대조
- [ ] `/privacy` 를 열어 `example.com` 이 한 글자도 안 남았는지 확인

> **랜딩만 열어보고 판정하지 않는다.** DB 가 잠들어도 랜딩은 200 이다.

---

## 아직 안 정한 것

출시 전에 정해야 하지만 **코드가 아니라 결정**이다.

| 무엇 | 어디 |
|---|---|
| 무료 초안에 교통수단을 어디까지 보여줄까 | 이슈 #53 🟡3 |
| 랜딩에서 값을 아예 안 보여주는 게 의도인가 | 이슈 #53 🟢 · PR #48 에서 남긴 질문 |
| 유료 전체 일정을 며칠 안에 준다고 약속할까 | 이슈 #51 |
| 환불 조건 | 위 15번 |
