-- 1인 예산을 숫자 대신 구간(글자)으로 받는다 (선경 요청, 2026-08-24).
--
-- 손님은 한국 물가를 몰라 숫자를 적기 어렵다. '10–15만원 (약 $75–110)' 처럼
-- 고르게 하면 그 자리에서 넘어가고, 우리도 정확한 액수보다 어느 급인지만 알면 일정을 짤 수 있다.
--
-- 옛 숫자 칸(budget_per_person)은 지우지 않는다 — 그 방식으로 들어온 초안이 남아 있다.

alter table public.plans
  add column budget_range text;
