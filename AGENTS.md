# 여행 컨설팅 사이트 — 팀 공통 규칙 v1.2

> 이 문서는 **이 저장소에서 일하는 모든 사람과 AI(Claude·Codex)** 에게 주는 규칙이다.
> 작성: 뮤조 / 최초 작성일: 2026-08-09 / 버전: 1.2
>
> 이 파일이 규칙의 유일한 원본이다. 따로 파일을 주고받지 않는다 — `git pull` 하면 항상 최신본이 따라온다.
> 규칙을 바꾸고 싶으면 이 파일을 고쳐서 PR로 올린다. (`CLAUDE.md`는 이 파일을 불러오기만 한다)

---

## 0. Claude가 지켜야 할 대화 규칙

- **선경과 뮤조 둘 다 개발 입문자다.** 모든 설명은 한국어로, 한 번에 한 단계씩.
- **전문 용어는 처음 나올 때 괄호로 뜻을 풀어 쓴다.**
  예: PR(Pull Request, 내 작업을 main에 합쳐달라고 올리는 요청서)
- 터미널 명령은 복사·붙여넣기 가능한 형태로 주고, **실행 전에 무엇을 하는 명령인지 한 줄로** 알려준다.
- 각 단계가 끝나면 "이게 제대로 됐는지 확인하는 법"을 알려주고, 확인된 뒤에 다음으로 넘어간다.
- 결제·삭제 등 **되돌리기 어려운 작업은 실행 전에 반드시 확인**받는다.
- 시키지 않은 작업을 미리 만들어두지 않는다. 다음 단계가 뭔지 애매하면 물어본다.

---

## 1. 프로젝트 개요

- **서비스**: 해외 여행객이 한국에 오기 전에 컨설팅을 받는 웹사이트
- **팀**: 선경 + 뮤조 2인. 둘 다 프론트엔드/백엔드 모두 참여
- **기술 스택**
  - Next.js (웹사이트를 만드는 도구)
  - TypeScript (자바스크립트에 "이건 숫자, 이건 글자" 표시를 붙여 실수를 미리 잡아주는 언어)
  - Tailwind CSS (디자인을 짧은 이름표로 붙이는 방식)
  - Supabase (데이터베이스 + 로그인 기능 서비스)
  - 배포(만든 사이트를 인터넷에 올리는 것) 방식은 아직 미정 — 정해지면 v1.1에 반영

### 계정 정리

| 서비스 | 소유자 | 뮤조 상태 |
|---|---|---|
| GitHub 저장소 `ssunie2/korea-travel-consulting` | 선경 (@ssunie2) | 초대 수락 완료 ✅ |
| Supabase 조직 / 프로젝트 `korea-travel-consulting` | 선경 | 초대 수락 완료 ✅ |
| 배포 서비스 | 선경 | 미정 |

---

## 2. 둘이 어떻게 같이 일하는가 (제일 중요)

**GitHub 저장소가 원본이고, 각자 컴퓨터에 있는 건 복사본이다.**
파일을 카톡이나 메일로 주고받지 않는다. 항상 GitHub를 거친다.

```
        GitHub (원본)
    ↑ push        ↓ pull
  뮤조 맥          선경 맥
```

- **클론(clone)**: GitHub 원본을 내 컴퓨터로 통째로 복사해오기 — 처음 한 번만
- **풀(pull)**: GitHub의 최신 상태를 내 컴퓨터로 당겨받기 — 작업 시작할 때마다
- **푸시(push)**: 내 작업을 GitHub로 올리기 — 작업 끝나면

⚠️ 구글 문서처럼 **실시간 동시 편집은 안 된다.** 각자 따로 작업하고 나중에 합치는 방식이다.

### 하루 작업 흐름 (둘 다 똑같이)

```bash
git checkout main && git pull        # ① 상대가 올린 최신 상태 받기
git checkout -b feat/내가-할-일       # ② 내 작업용 브랜치(작업 복사본) 만들기
# ... 작업 ...
git add . && git commit -m "무엇을 했는지 한 줄"   # ③ 저장 지점 찍기
git push -u origin feat/내가-할-일    # ④ GitHub에 올리기
```

④ 다음에 GitHub 웹사이트에서 **PR을 연다** → 상대가 **리뷰(코드 읽고 의견 남기기)** → 괜찮으면 **머지(main에 실제로 합치기)**.
머지가 끝나면 **다시 ①번부터.** 이게 계속 돌아간다.

---

## 3. 반드시 지킬 것 (금지 사항)

1. **main 브랜치에 직접 푸시 금지.** 무조건 새 브랜치 → PR → 상대 리뷰 → 머지
   **머지 버튼은 선경이 누른다** (자기 PR 포함). 이유가 두 가지다.
   - main에 무엇이 들어가는지 한 사람이 끝에서 확인한다
   - Vercel 무료 플랜은 팀원 초대가 안 돼서, **뮤조가 만든 커밋은 배포가 거부된다.** 선경이 머지해야 배포가 돈다
   승인 없이 머지하지 않는 것은 그대로다. 뮤조는 리뷰·승인까지만 하고 머지는 넘긴다
2. **데이터베이스 구조를 Supabase 대시보드에서 직접 바꾸지 않는다.**
   웹에서 테이블을 만들면 상대방 쪽에는 그 기록이 전혀 남지 않는다.
   → 반드시 `supabase/migrations/` 폴더 안에 SQL 파일로 남긴다 (이걸 마이그레이션이라고 한다)
3. **비밀번호·API 키를 git에 올리지 않는다.**
   실제 값은 `.env.local` 파일에 넣고, 그 파일은 git이 무시하도록 설정돼 있다.
   공유할 땐 `.env.example`에 **항목 이름만** 적어둔다. 값은 각자 Supabase 대시보드에서 복사해온다.
4. **남이 잡은 작업은 건드리지 않는다.** 같은 파일을 동시에 고치면 충돌(conflict, git이 누구 코드를 쓸지 몰라 멈추는 상태)이 난다.

---

## 4. 이름 규칙

| 종류 | 형식 | 예시 |
|---|---|---|
| 브랜치 — 새 기능 | `feat/기능이름` | `feat/consulting-form` |
| 브랜치 — 버그 수정 | `fix/버그이름` | `fix/login-error` |
| 브랜치 — 잡일·설정 | `chore/일이름` | `chore/eslint-setup` |
| 커밋 메시지 | 한국어로 무엇을 했는지 한 줄 | `예약 폼 화면 추가` |

---

## 5. 작업 분배

- **이슈(Issue, GitHub에 "이거 해야 함"을 카드처럼 적어두는 할 일 목록)** 를 쓴다
- **이슈 1개 = 브랜치 1개 = 담당 1명**
- 대화는 카톡 대신 **이슈나 PR 코멘트**에 남긴다. 나중에 "왜 이렇게 했더라" 할 때 기록이 남는다

---

## 6. 코드 규칙

- 지금 당장 필요 없는 기능·설정·추상화를 미리 만들지 않는다
- 새 라이브러리(남이 만든 코드 묶음)를 추가할 땐 PR 설명에 **왜 필요한지 한 줄** 적는다
- 표준 기능으로 되는 일에 라이브러리를 새로 깔지 않는다

---

## 7. 막혔을 때

- 에러 메시지는 **지우지 말고 그대로** Claude에게 보여준다. 요약하면 원인을 못 찾는다
- 뭔가 꼬였을 때 대부분은 되돌릴 수 있다. 혼자 지우기 전에 물어본다
- 상대가 올린 코드가 이해 안 되면 PR 코멘트로 질문한다. 모르는 채 승인하지 않는다

---

## 8. 폴더 구조 — 어떤 파일이 어디 사는지

```
app/                  화면. 폴더 이름이 그대로 주소가 된다
  page.tsx              → 첫 화면 (/)
  plan/page.tsx         → 여행 정보 입력 폼 (/plan)
  plan/[id]/page.tsx    → 결과 화면 (/plan/아이디)
  api/                → 서버에서만 도는 코드. AI 키처럼 숨겨야 할 건 전부 여기
  admin/              → 우리만 보는 관리자 화면
lib/                  여러 화면이 같이 쓰는 코드
  supabase-server.ts    → 데이터베이스 연결 (서버 전용)
components/           여러 화면이 같이 쓰는 화면 조각 (버튼, 카드 등)
supabase/migrations/  DB 구조 변경 기록 (SQL 파일)
```

`app/` 안에서 **폴더 이름 = 인터넷 주소**다. `app/plan/page.tsx`를 만들면 `우리사이트.com/plan`이 된다.

## 9. 충돌 안 나게 나눠 맡는 법

같은 파일을 둘이 동시에 고치면 충돌(conflict)이 난다. 그래서:

- **이슈마다 "내가 건드릴 파일"을 미리 적어둔다.** 겹치면 순서를 정하거나 한 명이 맡는다
- **자기 화면 폴더 안에서만 논다.** `app/plan/`을 맡았으면 그 안만 고친다
- **아래 파일들은 모두가 같이 쓰므로 조심한다.** 고쳐야 하면 이슈나 PR에 먼저 말한다

| 공용 파일 | 언제 건드리게 되나 |
|---|---|
| `package.json` | 새 라이브러리를 깔 때 |
| `app/layout.tsx` | 모든 화면에 공통으로 들어가는 것(머리말, 꼬리말)을 바꿀 때 |
| `app/globals.css` | 전체 색·글꼴을 바꿀 때 |
| `lib/` 안의 파일 | 공용 코드를 바꿀 때 |
| `AGENTS.md` | 규칙을 바꿀 때 |

- 작업 시작 전에 항상 `git checkout main && git pull` 로 최신을 받고 브랜치를 딴다. **오래된 상태에서 시작하는 게 충돌의 가장 큰 원인이다**

## 10. 지금 상태와 다음 할 일

- 2026-08-09 기준: 규칙 문서 + Next.js 뼈대까지 올라감. 기능 코드는 아직 없다
- 1차(MVP) 범위 10개는 이슈 #2 참고
- 다음 순서
  1. 기능별 이슈 10개 만들기
  2. 둘이 나눠 맡기
  3. DB 테이블 설계(C1)와 AI 서버(C2)를 먼저 — 나머지가 이 위에 얹힌다

---

## 변경 이력

| 버전 | 날짜 | 내용 |
|---|---|---|
| 1.0 | 2026-08-09 | 최초 작성 |
| 1.1 | 2026-08-09 | 뼈대 추가에 맞춰 폴더 구조(8장)와 충돌 방지 규칙(9장) 추가 |
| 1.2 | 2026-08-10 | 머지는 선경이 담당하는 것으로 명시 (3장) |

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
